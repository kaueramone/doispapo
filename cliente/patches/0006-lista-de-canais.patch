diff --git a/packages/client/src/interface/navigation/channels/ServerSidebar.tsx b/packages/client/src/interface/navigation/channels/ServerSidebar.tsx
index fa1ef5a..2db7af5 100644
--- a/packages/client/src/interface/navigation/channels/ServerSidebar.tsx
+++ b/packages/client/src/interface/navigation/channels/ServerSidebar.tsx
@@ -6,6 +6,7 @@ import {
   Setter,
   Show,
   Switch,
+  createEffect,
   createMemo,
 } from "solid-js";
 
@@ -18,7 +19,7 @@ import { KeybindAction, createKeybind } from "@revolt/keybinds";
 import { TextWithEmoji } from "@revolt/markdown";
 import { useModals } from "@revolt/modal";
 import { useNavigate } from "@revolt/routing";
-import { useVoice } from "@revolt/rtc";
+import { NOME_CANAL_AUSENTE, useVoice } from "@revolt/rtc";
 import { useState } from "@revolt/state";
 import {
   Column,
@@ -33,7 +34,9 @@ import {
   symbolSize,
   typography,
 } from "@revolt/ui";
+import { canalSobOPonteiro } from "@revolt/ui/components/features/voice/arrastar-usuario";
 import { VoiceChannelPreview } from "@revolt/ui/components/features/voice/VoiceChannelPreview";
+import { VoiceDock } from "@revolt/ui/components/features/voice/VoiceDock";
 import { createDragHandle } from "@revolt/ui/components/utils/Draggable";
 import { Symbol } from "@revolt/ui/components/utils/Symbol";
 
@@ -93,6 +96,34 @@ export const ServerSidebar = (props: Props) => {
   const navigate = useNavigate();
   const { isMobile } = useDevice();
 
+  /**
+   * Garante que o servidor tenha o canal de ausentes.
+   *
+   * Criado pelo cliente de quem TEM permissao, e nao por um processo no
+   * servidor: assim a criacao passa pela mesma API e pelas mesmas regras
+   * de permissao de qualquer canal, em vez de escrever no banco por fora.
+   *
+   * Roda uma vez por servidor por sessao. `criando` evita que duas
+   * renderizacoes seguidas criem dois canais iguais.
+   */
+  const criando = new Set<string>();
+  createEffect(() => {
+    const servidor = props.server;
+    if (!servidor || criando.has(servidor.id)) return;
+    if (!servidor.havePermission("ManageChannel")) return;
+
+    const existe = servidor.channels?.some(
+      (c) =>
+        c?.name?.trim().toLowerCase() === NOME_CANAL_AUSENTE.toLowerCase(),
+    );
+    if (existe) return;
+
+    criando.add(servidor.id);
+    servidor
+      .createChannel({ type: "Voice", name: NOME_CANAL_AUSENTE })
+      .catch(() => criando.delete(servidor.id));
+  });
+
   // Users can manage certain parts of the server individually, regardless of their ManageServer Permission
   const canManageServer = () =>
     props.server.orPermission(
@@ -252,6 +283,7 @@ export const ServerSidebar = (props: Props) => {
           )}
         </Draggable>
       </div>
+      <VoiceDock />
     </SidebarBase>
   );
 };
@@ -490,14 +522,47 @@ function Entry(
             : "normal",
   );
 
+  /**
+   * Recebe alguem arrastado de outro canal de voz.
+   *
+   * A soltura so avisa o servidor -- ele confere o cargo de quem pediu,
+   * descobre onde a pessoa esta e manda o cliente dela mudar de canal.
+   * Nada aqui decide se o movimento pode acontecer.
+   */
+  const alvoDeSoltura = () =>
+    props.channel.isVoice && canalSobOPonteiro() === props.channel.id;
+
   return (
     <Column gap="sm">
       <MenuButton
         href={`/server/${props.channel.serverId}/channel/${props.channel.id}`}
+        /*
+          A marca que faz deste canal um destino de soltura.
+
+          Quem procura o destino e o `arrastar-usuario.ts`, com
+          `elementFromPoint` sobre as coordenadas em que a pessoa soltou.
+          Nao ha `onDrop` porque nao ha arrasta-e-solta do navegador aqui
+          -- ele nunca chegou a comecar dentro da zona de reordenacao.
+        */
+        data-canal-voz={props.channel.isVoice ? props.channel.id : undefined}
+        attention={alvoDeSoltura() ? "selected" : attentionState()}
+        /*
+          Double click on a voice channel connects; single click just opens it
+          so you can read along without joining. Connecting while already
+          connected somewhere else is a straight swap — `connect` disconnects
+          first — so moving between rooms is one gesture, not leave-then-join.
+
+          Already in this room, double click does nothing: leaving is a
+          deliberate act and belongs to the button in the dock.
+        */
+        onDblClick={
+          props.channel.isVoice && !inCall()
+            ? () => voice.connect(props.channel)
+            : undefined
+        }
         use:floating={props.menuGenerator(props.channel)}
         size="normal"
         alert={alertState()}
-        attention={attentionState()}
         icon={
           <>
             <Switch fallback={<Symbol>grid_3x3</Symbol>}>
